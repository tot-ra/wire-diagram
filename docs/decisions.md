# Initial decisions

- User requested a reusable library plus a runnable demo, not a website-only implementation.
- User accepted the recommended approach; YAML is the initial authoring format, with a versioned schema and a typed JavaScript API. This is not WireViz syntax compatibility.
- User selected a widget with SVG for the schematic and a canvas for 3D, rather than literally one canvas for all UI.
- Framework-independent TypeScript, Three.js loaded only on demand, Vite library build.
- Both views consume one validated, pin-level graph. Repeated endpoint references form junctions. A resistor remains a component, never a direct short between its terminals.
- Physical units are millimetres, schematic coordinates are independent SVG units. Built-in models are illustrative, not verified manufacturing CAD. External glTF/GLB models require author-supplied local pin anchors and scale.
- Work stays in this repository. The Gratheon reference is read-only; integration is documented separately.
- Demo page review (2026-09-16): rebuild the 20 kg load cell as a bar/I-beam, enlarge the canvas, cut secondary copy, remove assembly notes and BOM from widget chrome, drop the marketing hero, and keep YAML as a compact editor only. `getBom` stays in the API.
- Browser checks are allowed for this visual pass. The earlier "no browser" request applied to the initial library delivery, not to later screenshot reviews.
- Schematic wheel zoom keeps the diagram point under the cursor fixed. Scaling only the transform previously zoomed around the SVG origin (top-left).
- Screenshot annotation 1 (2026-09-16, USB power supply gray/black dashes on the blue part) maps to the 3D `kind: power` brick. The blue badge shared the housing top plane and z-fought with the dark body.
- Screenshot annotation 1 (2026-09-16, improve ESP32 3D model to have pins with wires on them) maps to `kind: esp32`. The previous model used two short header bars on the long edges while demo pin anchors sat at y=3 and, for VIN/GND/3V3, beyond the bar length. The built-in model now has a 19-pin dual header at 2.54 mm pitch; demo anchors sit on the gold pin tops. Schematic `side` still means left/right; in 3D those rows are +Z and -Z.
- Canvas chrome (2026-09-16): Schematic/3D becomes an iOS-style switch in the canvas top-right; Reset is a link under that switch; Export SVG moves to the canvas bottom-right; the inspector/select sidebar stays hidden until a component or wire is selected.
- Entrance Observer Phase 1 BOM (2026-09-16): add built-in kinds for Jetson, USB camera, CS lens, M.2 SSD, WiFi NIC, HDMI display, camera mount, 2020 extrusion, acrylic cover and paddle antenna. These are product-interconnect silhouettes, not GPIO pin maps. Keep Jetson USB-C at PD 19 V; do not copy the ESP32 5 V lab map.
- `kind: display` stands on Y with the screen on +Z. A 7 inch panel authored as `[165, 8, 100]` lies flat and hides the Jetson in the default 3D camera.
- 3D silhouettes are modules under `src/models/builtin/`, not inlines in `view3d.ts`. Third parties register extra or replacement kinds with `registerModel` or widget `models`. YAML `kind` is any lowercase kebab-case id; unknown kinds fall back to the generic board. Built-in registration skips kinds already registered so a lazy 3D import cannot clobber a plugin.
- Catalog expansion (2026-09-16): add AliExpress-style `max4466` / `max9814` mic amps, `ds18b20` 3-pin module (keep waterproof capsules on `probe`), `lcd1602` I2C (flat, glass on +Y; standing HDMI panels stay `display`), `raspberry-pi` with `properties.variant` `4`/`5`/`zero`/`pico`, passive `barrel-jack` and `jst-connector`, `stepper-motor` + `stepper-driver`, and `led` / `status-led`. These are illustrative silhouettes, not verified pin maps.
- Showcase gallery (2026-09-16): `?demo=showcase` renders one compact 3D canvas per catalog part, grouped under YAML section headings starting with "Beehive sensor bench", instead of one crowded scene. Demo code keeps at most twelve live WebGL widgets, preferring document order among on-screen cards, because browsers cap concurrent contexts. `fitCamera()` uses a longer distance for one-component diagrams so square gallery cards do not clip cubes.
- Single demo page (2026-09-16): screenshot annotations 1-2 on `?demo=showcase` map to the header Demo picker and the Edit source jump link. Drop both. One page now stacks the beehive widget, the 3D catalog, and the YAML editor. Apply/Restore still edit only `beehive.yaml`; the catalog stays sourced from `showcase.yaml`.
- Raspberry Pi 3D (2026-09-16): GPIO was a solid black bar and USB-A was one dark box. Pi 4/5 now use a 2x20 2.54 mm header with gold contacts standing proud of the housing (stacking-header silhouette; recessed female wells vanished from the default +Z camera), plus two stacked USB-A shells (USB 3 blue, USB 2 black) with cavities that cross the metal face. Pico uses dual male 20-pin rails; Zero keeps the same GPIO and two micro-USB ports. Header and USB builders live in `src/models/parts/` and are reused on ESP32 rails, Jetson/camera USB, and short breakout headers. Connector metal uses moderate metalness because this scene has no environment map.
- Raspberry Pi 4 3D (2026-09-16): screenshot-style request to make `kind: raspberry-pi` variant `4` more realistic, with AliExpress item 1005011525330407 as the listing. The listing was traffic-blocked; the silhouette follows the official 85 x 56 mm mechanical drawing (mounting holes at 3.5 / 58 / 49 mm, GPIO centre at 32.5 mm, dual micro-HDMI, USB-C, TRRS jack, magjack with LEDs, CSI/DSI, silver BCM2711 lid, RAM, PoE header, underside microSD). Illustrative, not a verified pin map.
- LED 3D (2026-09-16): screenshot annotation 1 on `?demo=showcase` (5 mm LED) maps to `kind: led`. Default pin markers sat on the red body sides. The model now puts the anode post and cathode cup under the epoxy on the metal leads; wire anchors (`resolvePinPosition`) land on those legs and yellow pin markers are hidden.
- Displays group (2026-09-16): screenshot annotation 1 on `?demo=showcase` asked to move displays under their own group. HDMI `kind: display` leaves Enclosure and `lcd1602` leaves Sensor modules; both sit in YAML group `displays` (heading Displays).
- LCD1602 3D (2026-09-16): the same annotation asked for a more realistic LCD. The silhouette is now a green PCB with corner holes, a nickel can, a four-rail black window (not a solid lid), backlit 16x2 cells raised off the glass, and a PCF8574 backpack that peeks past +Z with a blue contrast pot and 4-pin I2C header. Glass stays on +Y; standing HDMI panels stay `kind: display`.
- LCD2004 I2C (2026-09-16): add `kind: lcd2004` as the 20x4 sibling of `lcd1602` (AliExpress 2004A + PCF8574 backpack, typical 98 x 60 mm). Shared character-LCD builder; 2004 uses a ~3:1 window (76 x 25.2 mm) instead of 1602's ~4:1. Same I2C pins GND/VCC/SDA/SCL. Illustrative silhouette, not a verified pin map.
- DS18B20 3D (2026-09-16): screenshot annotation 1 on `?demo=showcase` asked to make the DS18B20 module more realistic, with AliExpress item 1005009039491680 as the target. The listing was traffic-blocked; the silhouette follows the common 21 x 10 mm blue breakout (D-shaped TO-92 on standoff leads, 103/102 SMD, power LED, three separate Dupont pins, far-end copper pads). Waterproof capsules stay `kind: probe`. Illustrative, not a verified pin map.
- Arduino UNO R3 (2026-09-16): add `kind: arduino-uno` as a detailed development-board silhouette (teal 68.6 x 53.3 mm PCB, USB Type-B, DC barrel jack, DIP ATmega328P in a socket, 16U2, female digital/power/analog headers on a 100-mil grid, ICSP, reset, electrolytics, ON/L/TX/RX LEDs). Schematic left maps to digital +Z, right to power/analog -Z; named pin ids land on the matching female socket. Illustrative, not a verified pin map.
- ESP32 DevKit 3D (2026-09-16): make `kind: esp32` more realistic, with AliExpress item 1005011654004220 as the listing. The listing was traffic-blocked; the silhouette follows the common 52 x 28 mm USB-C ESP32-WROOM-32 DevKit clone (18 x 25.5 x 3.1 mm module and inverted-F antenna, dual 19-pin males, EN/BOOT, CH340, AMS1117, power/TX/RX LEDs). Connector metal stays around metalness 0.4. Illustrative, not a verified pin map.
